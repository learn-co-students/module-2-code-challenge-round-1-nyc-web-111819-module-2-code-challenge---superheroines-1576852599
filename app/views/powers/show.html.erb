<h2>Power Show Page</h2>

<p>Name of Power: <%= @power.name %></p>
<p>Description of Power: <%= @power.description %></p>

<ul>
    <p>All heroines with this power:</p>
    <% @power.heroines.each do |heroine| %>
        <li><%= link_to heroine.name, heroine_path(heroine) %></li>
    <% end %>
</ul>

<%= button_to "All Powers", powers_path, method: :get %>
<%= button_to "Edit Power", edit_power_path(@power), method: :get %>