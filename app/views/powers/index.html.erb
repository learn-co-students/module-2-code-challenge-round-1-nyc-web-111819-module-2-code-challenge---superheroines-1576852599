<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <strong><%= link_to(power.name, power_path(power)) %></strong>
      <br>
      Description: <%= power.description %>
    </li>
    <br>
  <% end %>
</ul>
<hr>
<%= link_to("View List of All Heroines", heroines_path) %>